import { EmployeeRow } from './excelService';
interface ValidationError {
    row: number;
    field: string;
    value: string;
    message: string;
    severity: 'error' | 'warning' | 'info';
}
interface ValidationReport {
    totalRows: number;
    validRows: number;
    errorRows: number;
    warningRows: number;
    autoGenerated: number;
    errors: ValidationError[];
    leavePlanDistribution: Record<string, number>;
}
export declare class BulkUploadService {
    validateEmployees(employees: EmployeeRow[]): Promise<ValidationReport>;
    processEmployees(employees: EmployeeRow[], onProgress?: (current: number, total: number, employeeName: string) => void): Promise<void>;
    private filterValidEmployees;
    private generateEmployeeId;
    private isValidEmail;
    private calculateAge;
}
export declare const bulkUploadService: BulkUploadService;
export {};
//# sourceMappingURL=bulkUploadService.d.ts.map